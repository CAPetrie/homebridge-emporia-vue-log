# homebridge-emporia-vue-log

Emporia Vue virtual switch plug-in (integration) that relies on Emporia Vue API to monitor and log the power currently being consumed and displays it both to the terminal and in HomeKit via the lux parameter of a switch.
